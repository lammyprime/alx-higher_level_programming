#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    newdictionary = a_dictionary.copy()
    for i in range(len(a_dictionary)):
        newdictionary[i] = list(map(lambda x: x * 2, a_dictionary[i]))
    return newdictionary
