#!/usr/bin/python3
def search_replace(my_list, search, replace):
    newlist = my_list.copy()
    for i in range(len(my_list)):
        if newlist[i] == replace:
            newlist[i] = search
    return newlist
